import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

export default function ToolBar() {
  return (
    <Card className="m-3 h-24 absolute bottom-0 left-0 right-0 bg-gray-100 border-1">
      <CardHeader>
        <CardTitle>Toolbar</CardTitle>
      </CardHeader>
      <CardContent>
        
      </CardContent>
    </Card>
  );
}
